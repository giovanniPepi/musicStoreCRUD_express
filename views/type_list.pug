extends layout 

block content 
  h1=title

  ul 
    each type in type_list 
      li 
        a(href=type.url) #{type.name}
        p #{type.description}
        
    else 
      li There are no types!