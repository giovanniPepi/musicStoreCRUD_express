extends layout

block content
  h1= title
  p Welcome to #[em Music Store CRUD!]

  if error 
    p Error getting data from db 
  else 
    h3 Database Statistics: 

    ul 
      li #[strong Instruments]: !{data.instrument_count}
      li #[strong Types]: !{data.type_count}
      li #[strong Brands]: !{data.brand_count}

